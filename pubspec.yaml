name: fruit_hub_dashboard
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.4.3 <4.0.0'




dependencies:
  cached_network_image: ^3.4.1
  cloud_firestore: ^4.15.9   # Last version to support SDK 34
  dartz: ^0.10.1
  firebase_core: ^2.24.2     # Last version before v3 (which requires SDK 35)
  firebase_storage: ^11.6.3  # Compatible with firebase_core v2
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.6
  get_it: ^7.7.0
  image_picker: ^1.0.7       # Avoid 1.1.x, which requires SDK 35
  modal_progress_hud_nsn: ^0.5.1
  path: ^1.9.0
  skeletonizer: ^2.0.1
  supabase_flutter: ^2.8.0   # ✅ This one supports SDK 34 as of now



dev_dependencies:
  flutter_lints: ^3.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
